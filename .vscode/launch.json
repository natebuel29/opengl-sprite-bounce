{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "program": "${workspaceFolder}/build/Debug/spriteBounce.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}